---
source: https://www.hiascend.com/doc_center/source/zh/CANNCommunityEdition/82RC1/API/ascendcopapi/atlasascendc_api_07_1126.html
converted: true
---

# AllocScalarList

#### 功能说明
申请一个aclScalarList，并指定其内部包含的aclScalar。

#### 函数原型
**aclScalarList *AllocScalarList(const aclScalar *const *scalars, uint64_t size)**

#### 参数说明
<table border="1">
<thead><tr><th>参数</th><th>输入/输出</th><th>说明</th></tr></thead>
<tbody><tr><td>scalars</td><td>输入</td><td>源数据，用于初始化aclScalarList。</td></tr><tr><td>size</td><td>输入</td><td>源数据的元素个数。</td></tr></tbody>
</table>

#### 返回值说明
返回申请到的aclScalarList对象，申请失败返回nullptr。

#### 约束说明
入参指针不能为空。

#### 调用示例
```cpp
// 初始化5个aclScalar，并将他们组装为一个aclScalarList
void Func(aclOpExecutor *executor) {
    int64_t val = 5;
    std::vector<aclScalar *> scalars;
    for (int64_t i = 1; i <= 5; i++) {
        aclScalar *scalar = executor->AllocScalar(val);
        scalars.push_back(scalar);
    }
    aclScalarList *scalarList = executor->AllocScalarList(scalars.data(), scalars.size());
}
```