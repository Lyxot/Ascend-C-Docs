---
source: https://www.hiascend.com/doc_center/source/zh/CANNCommunityEdition/82RC1/API/ascendcopapi/atlasascendc_api_07_0022.html
converted: true
---

# ToFloat

#### 产品支持情况
<table border="1" rules="all" cellpadding="4" cellspacing="0"><thead><tr><th>产品</th><th>是否支持</th></tr></thead><tbody><tr><td>Atlas A3 训练系列产品/Atlas A3 推理系列产品</td><td>√</td></tr><tr><td>Atlas A2 训练系列产品/Atlas 800I A2 推理产品/A200I A2 Box 异构组件</td><td>√</td></tr><tr><td>Atlas 200I/500 A2 推理产品</td><td>x</td></tr><tr><td>Atlas 推理系列产品AI Core</td><td>x</td></tr><tr><td>Atlas 推理系列产品Vector Core</td><td>x</td></tr><tr><td>Atlas 训练系列产品</td><td>x</td></tr><tr><td>Atlas 200/300/500 推理产品</td><td>x</td></tr></tbody></table>

#### 功能说明
将输入数据转换为float类型。

#### 函数原型
- bfloat16_t类型转换为float类型

```cpp
__aicore__ inline float ToFloat(const bfloat16_t& bVal)
```

#### 参数说明
<table border="1" rules="all" cellpadding="4" cellspacing="0"><caption>表1 接口参数说明</caption><thead><tr><th>参数名称</th><th>输入/输出</th><th>含义</th></tr></thead><tbody><tr><td>bVal</td><td>输入</td><td>待转换的标量数据。</td></tr></tbody></table>

#### 约束说明
无

#### 返回值
转换后的float类型标量数据。

#### 调用示例
```cpp
void CalcFunc(bfloat16_t n)
{
  int dataLen = 32;
  AscendC::TPipe pipe;
  AscendC::TQue<AscendC::TPosition::VECIN, 1> inQueueSrcVecIn;
  AscendC::TQue<AscendC::TPosition::VECOUT, 1> inQueueDstVecIn;
  pipe.InitBuffer(inQueueDstVecIn, 1, dataLen * sizeof(bfloat16_t));
  pipe.InitBuffer(inQueueSrcVecIn, 1, dataLen * sizeof(float));
  AscendC::LocalTensor<bfloat16_t> dstLocal = inQueueDstVecIn.AllocTensor<bfloat16_t>();
  AscendC::LocalTensor<float> srcLocal = inQueueSrcVecIn.AllocTensor<float>();
  float t = AscendC::ToFloat(n); // 对标量进行加法，不支持bfloat16_t，需要先转换成float
  PipeBarrier<PIPE_ALL>();
  AscendC::Duplicate(srcLocal, float(4.0f), dataLen);
  PipeBarrier<PIPE_ALL>();
  Adds(srcLocal, srcLocal, t, dataLen);
  PipeBarrier<PIPE_ALL>();
  // 做加法运算后，输出bfloat16_t类型tensor
  Cast(dstLocal, srcLocal, AscendC::RoundMode::CAST_ROUND, dataLen);
  // ……
}
```