---
source: https://www.hiascend.com/doc_center/source/zh/CANNCommunityEdition/82RC1/devaids/optool/atlasopdevapi_16_0013.html
converted: true
---

# vector_dup

#### 功能说明

vector_dup指令抽象。

y = vector_dup(x)， x、 y按元素填充元素。

#### 接口原型

```python
class vector_dup(x, y, fill_shape)
```

#### 参数说明

<table cellpadding="4" cellspacing="0" border="1" frame="border" rules="all"><thead><tr><th>参数名</th><th>输入/输出</th><th>数据类型</th><th>说明</th></tr></thead><tbody><tr><td>x</td><td>输入</td><td>Tensor变量</td><td>输入x向量Tensor。支持FP16、FP32、INT16、INT32、UINT16、UINT32。</td></tr><tr><td>y</td><td>输出</td><td>Tensor变量</td><td>输出y向量Tensor。支持FP16、FP32、INT16、INT32、UINT16、UINT32。</td></tr><tr><td>fill_shape</td><td>输入</td><td>list</td><td>表示目标Tensor的要被扩充的shape值。</td></tr></tbody></table>

#### 约束说明

由于该指令输入仅一个标量，因此需要创建一个shape为[1]且is_inited=True的Tensor作为模拟标量输入，不增加性能开销。

#### 使用示例

```python
from mskpp import vector_dup, Tensor
ub_x = Tensor("UB", "FP16", [1], format="ND", is_inited=True)
ub_y = Tensor("UB")
out = vector_dup(ub_x, ub_y, [8, 2048])()
```